#Step 1: Setup (Virtual Environment + Installation)
  
    sudo apt update
    sudo apt install python3 python3-pip -y

  
#Step 2 Setup virtual environment (optional but clean)

    python3 -m venv venv
    source venv/bin/activate


#Step 3 Install Checkov
      
    pip install checkov


#Step 4: Common Checkov Scanning Commands

    # Scan current directory (auto-detect IaC types)
    checkov -d .
    # Scan a specific Terraform file
    checkov -f main.tf
